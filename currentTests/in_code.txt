int main () {
    int x=0;
    int y =5;
    x++;
    --y;
    x = x + y;
    x = x - y;
    x = x * y;
    x = x / y;
    x = x & y;
    x = x | y;
    x = x ^ y;
    x = y;
    x = x == y;
    x = y;
    x = x != y;
    return x;
}
